# coding: utf-8

"""
    VRChat API Documentation


    The version of the OpenAPI document: 1.20.8
    Contact: vrchatapi.lpv0t@aries.fyi
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from collections.abc import Mapping as _Mapping
from datetime import datetime
from pydantic import AliasChoices, BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, cast as _cast
from typing_extensions import Annotated
from vrchatapi.models.jam_state_change_dates import JamStateChangeDates
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


_OPENAPI_GENERATOR_TO_DICT = "_openapi_generator_to_dict"


def _get_openapi_to_dict(value: Any) -> Any:
    # Reciprocal function references preserve inherited generated methods
    # without reserving model member names. Checking both directions also
    # rejects overrides whose decorators copy function attributes.
    to_dict = getattr(value, "to_dict", None)
    type_to_dict = getattr(type(value), "to_dict", None)
    if (
        not callable(to_dict)
        or getattr(to_dict, "__func__", None) is not type_to_dict
    ):
        return None

    openapi_to_dict = getattr(
        type_to_dict, _OPENAPI_GENERATOR_TO_DICT, None
    )
    if (
        not callable(openapi_to_dict)
        or getattr(
            openapi_to_dict,
            _OPENAPI_GENERATOR_TO_DICT,
            None,
        ) is not type_to_dict
    ):
        return None
    return openapi_to_dict


def _to_legacy_item(value: Any, serialize: bool) -> Any:
    to_dict = getattr(value, "to_dict", None)
    if _get_openapi_to_dict(value) is not None and callable(to_dict):
        return to_dict(serialize=serialize)
    if callable(to_dict):
        return to_dict()
    return value


def _to_legacy_value(value: Any, serialize: bool) -> Any:
    # python-legacy converted only immediate list elements or dictionary values:
    # https://github.com/OpenAPITools/openapi-generator/blob/c84b949df1a9ec04ba75989cb49b45c940c2f694/modules/openapi-generator/src/main/resources/python-legacy/model.mustache#L203-L231
    if isinstance(value, list):
        return [_to_legacy_item(item, serialize) for item in value]
    if isinstance(value, dict):
        return {
            key: _to_legacy_item(item, serialize)
            for key, item in value.items()
        }
    return _to_legacy_item(value, serialize)


def _to_openapi_value(value: Any) -> Any:
    if isinstance(value, list):
        return [_to_openapi_value(item) for item in value]
    if isinstance(value, dict):
        return {key: _to_openapi_value(item) for key, item in value.items()}

    to_openapi_dict = _get_openapi_to_dict(value)
    if to_openapi_dict is not None:
        return to_openapi_dict(value)

    to_dict = getattr(value, "to_dict", None)
    if callable(to_dict):
        return to_dict()
    return value


class Jam(BaseModel):
    """
    

    Attributes:
      openapi_types (dict): The key is attribute name
                            and the value is attribute type.
      attribute_map (dict): The key is attribute name
                            and the value is json key in definition.
    """ # noqa: E501
    description: Optional[Annotated[str, Field(min_length=1, strict=True)]] = None
    id: Optional[StrictStr] = Field(default=None, json_schema_extra={"examples": ["jam_0b7e3f6d-4647-4648-b2a1-1431e76906d9"]})
    is_visible: Optional[StrictBool] = Field(default=None, validation_alias=AliasChoices("isVisible", "is_visible"), serialization_alias="isVisible")
    more_info: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, validation_alias=AliasChoices("moreInfo", "more_info"), serialization_alias="moreInfo")
    state: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, description="One of: - submissions_open - closed")
    state_change_dates: Optional[JamStateChangeDates] = Field(default=None, validation_alias=AliasChoices("stateChangeDates", "state_change_dates"), serialization_alias="stateChangeDates")
    submission_content_gate_date: Optional[datetime] = Field(default=None, validation_alias=AliasChoices("submissionContentGateDate", "submission_content_gate_date"), serialization_alias="submissionContentGateDate")
    submission_content_gated: Optional[StrictBool] = Field(default=None, validation_alias=AliasChoices("submissionContentGated", "submission_content_gated"), serialization_alias="submissionContentGated")
    title: Optional[Annotated[str, Field(min_length=1, strict=True)]] = None
    updated_at: Optional[datetime] = None
    openapi_types: ClassVar[Dict[str, str]] = {
        "description": "str",
        "id": "str",
        "is_visible": "bool",
        "more_info": "str",
        "state": "str",
        "state_change_dates": "JamStateChangeDates",
        "submission_content_gate_date": "datetime",
        "submission_content_gated": "bool",
        "title": "str",
        "updated_at": "datetime"
    }

    attribute_map: ClassVar[Dict[str, str]] = {
        "description": "description",
        "id": "id",
        "is_visible": "isVisible",
        "more_info": "moreInfo",
        "state": "state",
        "state_change_dates": "stateChangeDates",
        "submission_content_gate_date": "submissionContentGateDate",
        "submission_content_gated": "submissionContentGated",
        "title": "title",
        "updated_at": "updated_at"
    }
    __properties: ClassVar[List[str]] = ["description", "id", "isVisible", "moreInfo", "state", "stateChangeDates", "submissionContentGateDate", "submissionContentGated", "title", "updated_at"]

    @classmethod
    def __preprocess_input_names(
        cls,
        obj: Any,
        remove_hidden_storage_names: bool = True,
    ) -> Any:
        if not isinstance(obj, _Mapping):
            return obj
        obj = dict(obj)
        if "isVisible" not in obj and "is_visible" in obj:
            obj["isVisible"] = obj["is_visible"]
        obj.pop("is_visible", None)
        if "moreInfo" not in obj and "more_info" in obj:
            obj["moreInfo"] = obj["more_info"]
        obj.pop("more_info", None)
        if "stateChangeDates" not in obj and "state_change_dates" in obj:
            obj["stateChangeDates"] = obj["state_change_dates"]
        obj.pop("state_change_dates", None)
        if "submissionContentGateDate" not in obj and "submission_content_gate_date" in obj:
            obj["submissionContentGateDate"] = obj["submission_content_gate_date"]
        obj.pop("submission_content_gate_date", None)
        if "submissionContentGated" not in obj and "submission_content_gated" in obj:
            obj["submissionContentGated"] = obj["submission_content_gated"]
        obj.pop("submission_content_gated", None)
        return obj

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        extra="forbid",
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model"""
        return pprint.pformat(self.to_dict())

    def __repr__(self) -> str:
        """For print and pprint"""
        return self.to_str()

    def __eq__(self, other: object) -> bool:
        """Returns true if both objects are equal"""
        if not isinstance(other, Jam):
            return False

        return self.to_dict() == other.to_dict()

    def __ne__(self, other: object) -> bool:
        """Returns true if both objects are not equal"""
        if not isinstance(other, Jam):
            return True

        return not (self == other)


    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        to_openapi_dict = _get_openapi_to_dict(self)
        if to_openapi_dict is not None:
            return json.dumps(to_jsonable_python(to_openapi_dict(self)))
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Jam from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, serialize: bool = False) -> Dict[str, Any]:
        """Return all declared model fields using public or wire names."""
        return {
            ("description" if serialize else "description"): _to_legacy_value(getattr(self, "description", None), serialize),
            ("id" if serialize else "id"): _to_legacy_value(getattr(self, "id", None), serialize),
            ("isVisible" if serialize else "is_visible"): _to_legacy_value(getattr(self, "is_visible", None), serialize),
            ("moreInfo" if serialize else "more_info"): _to_legacy_value(getattr(self, "more_info", None), serialize),
            ("state" if serialize else "state"): _to_legacy_value(getattr(self, "state", None), serialize),
            ("stateChangeDates" if serialize else "state_change_dates"): _to_legacy_value(getattr(self, "state_change_dates", None), serialize),
            ("submissionContentGateDate" if serialize else "submission_content_gate_date"): _to_legacy_value(getattr(self, "submission_content_gate_date", None), serialize),
            ("submissionContentGated" if serialize else "submission_content_gated"): _to_legacy_value(getattr(self, "submission_content_gated", None), serialize),
            ("title" if serialize else "title"): _to_legacy_value(getattr(self, "title", None), serialize),
            ("updated_at" if serialize else "updated_at"): _to_legacy_value(getattr(self, "updated_at", None), serialize),
        }

    def __openapi_generator_modern_projection(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of state_change_dates
        if self.state_change_dates:
            _dict['stateChangeDates'] = _to_openapi_value(self.state_change_dates)
        # set to None if submission_content_gate_date (nullable) is None
        # and model_fields_set contains the field
        if self.submission_content_gate_date is None and "submission_content_gate_date" in self.model_fields_set:
            _dict['submissionContentGateDate'] = None

        return _dict

    setattr(
        to_dict,
        _OPENAPI_GENERATOR_TO_DICT,
        __openapi_generator_modern_projection,
    )
    setattr(
        __openapi_generator_modern_projection,
        _OPENAPI_GENERATOR_TO_DICT,
        to_dict,
    )
    del __openapi_generator_modern_projection

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Jam from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        obj = _cast(
            Dict[str, Any],
            cls.__preprocess_input_names(
                obj,
                remove_hidden_storage_names=True,
            ),
        )

        _obj = cls.model_validate({
            "description": obj.get("description"),
            "id": obj.get("id"),
            "isVisible": obj.get("isVisible"),
            "moreInfo": obj.get("moreInfo"),
            "state": obj.get("state"),
            "stateChangeDates": JamStateChangeDates.from_dict(obj["stateChangeDates"]) if obj.get("stateChangeDates") is not None else None,
            "submissionContentGateDate": obj.get("submissionContentGateDate"),
            "submissionContentGated": obj.get("submissionContentGated"),
            "title": obj.get("title"),
            "updated_at": obj.get("updated_at")
        })
        return _obj


