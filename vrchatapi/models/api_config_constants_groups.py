# coding: utf-8

"""
    VRChat API Documentation


    The version of the OpenAPI document: 1.20.8
    Contact: vrchatapi.lpv0t@aries.fyi
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from collections.abc import Mapping as _Mapping
from pydantic import AliasChoices, BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, cast as _cast
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


_OPENAPI_GENERATOR_TO_DICT = "_openapi_generator_to_dict"


def _get_openapi_to_dict(value: Any) -> Any:
    # Reciprocal function references preserve inherited generated methods
    # without reserving model member names. Checking both directions also
    # rejects overrides whose decorators copy function attributes.
    to_dict = getattr(value, "to_dict", None)
    type_to_dict = getattr(type(value), "to_dict", None)
    if (
        not callable(to_dict)
        or getattr(to_dict, "__func__", None) is not type_to_dict
    ):
        return None

    openapi_to_dict = getattr(
        type_to_dict, _OPENAPI_GENERATOR_TO_DICT, None
    )
    if (
        not callable(openapi_to_dict)
        or getattr(
            openapi_to_dict,
            _OPENAPI_GENERATOR_TO_DICT,
            None,
        ) is not type_to_dict
    ):
        return None
    return openapi_to_dict


def _to_legacy_item(value: Any, serialize: bool) -> Any:
    to_dict = getattr(value, "to_dict", None)
    if _get_openapi_to_dict(value) is not None and callable(to_dict):
        return to_dict(serialize=serialize)
    if callable(to_dict):
        return to_dict()
    return value


def _to_legacy_value(value: Any, serialize: bool) -> Any:
    # python-legacy converted only immediate list elements or dictionary values:
    # https://github.com/OpenAPITools/openapi-generator/blob/c84b949df1a9ec04ba75989cb49b45c940c2f694/modules/openapi-generator/src/main/resources/python-legacy/model.mustache#L203-L231
    if isinstance(value, list):
        return [_to_legacy_item(item, serialize) for item in value]
    if isinstance(value, dict):
        return {
            key: _to_legacy_item(item, serialize)
            for key, item in value.items()
        }
    return _to_legacy_item(value, serialize)


def _to_openapi_value(value: Any) -> Any:
    if isinstance(value, list):
        return [_to_openapi_value(item) for item in value]
    if isinstance(value, dict):
        return {key: _to_openapi_value(item) for key, item in value.items()}

    to_openapi_dict = _get_openapi_to_dict(value)
    if to_openapi_dict is not None:
        return to_openapi_dict(value)

    to_dict = getattr(value, "to_dict", None)
    if callable(to_dict):
        return to_dict()
    return value


class APIConfigConstantsGROUPS(BaseModel):
    """
    Group-related constants

    Attributes:
      openapi_types (dict): The key is attribute name
                            and the value is attribute type.
      attribute_map (dict): The key is attribute name
                            and the value is json key in definition.
    """ # noqa: E501
    capacity: Optional[StrictInt] = Field(default=100000, validation_alias=AliasChoices("CAPACITY", "capacity"), serialization_alias="CAPACITY", description="Maximum group capacity")
    group_transfer_requirements: Optional[List[StrictStr]] = Field(default=None, validation_alias=AliasChoices("GROUP_TRANSFER_REQUIREMENTS", "group_transfer_requirements"), serialization_alias="GROUP_TRANSFER_REQUIREMENTS", description="Requirements for transferring group ownership")
    max_invites_requests: Optional[StrictInt] = Field(default=50, validation_alias=AliasChoices("MAX_INVITES_REQUESTS", "max_invites_requests"), serialization_alias="MAX_INVITES_REQUESTS", description="Maximum number of invite requests")
    max_joined: Optional[StrictInt] = Field(default=100, validation_alias=AliasChoices("MAX_JOINED", "max_joined"), serialization_alias="MAX_JOINED", description="Maximum number of joined groups")
    max_joined_plus: Optional[StrictInt] = Field(default=200, validation_alias=AliasChoices("MAX_JOINED_PLUS", "max_joined_plus"), serialization_alias="MAX_JOINED_PLUS", description="Maximum number of joined groups for VRChat Plus members")
    max_languages: Optional[StrictInt] = Field(default=10, validation_alias=AliasChoices("MAX_LANGUAGES", "max_languages"), serialization_alias="MAX_LANGUAGES", description="Maximum number of supported languages")
    max_links: Optional[StrictInt] = Field(default=3, validation_alias=AliasChoices("MAX_LINKS", "max_links"), serialization_alias="MAX_LINKS", description="Maximum number of group links")
    max_management_roles: Optional[StrictInt] = Field(default=5, validation_alias=AliasChoices("MAX_MANAGEMENT_ROLES", "max_management_roles"), serialization_alias="MAX_MANAGEMENT_ROLES", description="Maximum number of management roles in a group")
    max_owned: Optional[StrictInt] = Field(default=5, validation_alias=AliasChoices("MAX_OWNED", "max_owned"), serialization_alias="MAX_OWNED", description="Maximum number of groups a user can own")
    max_roles: Optional[StrictInt] = Field(default=50, validation_alias=AliasChoices("MAX_ROLES", "max_roles"), serialization_alias="MAX_ROLES", description="Maximum number of roles in a group")
    openapi_types: ClassVar[Dict[str, str]] = {
        "capacity": "int",
        "group_transfer_requirements": "List[str]",
        "max_invites_requests": "int",
        "max_joined": "int",
        "max_joined_plus": "int",
        "max_languages": "int",
        "max_links": "int",
        "max_management_roles": "int",
        "max_owned": "int",
        "max_roles": "int"
    }

    attribute_map: ClassVar[Dict[str, str]] = {
        "capacity": "CAPACITY",
        "group_transfer_requirements": "GROUP_TRANSFER_REQUIREMENTS",
        "max_invites_requests": "MAX_INVITES_REQUESTS",
        "max_joined": "MAX_JOINED",
        "max_joined_plus": "MAX_JOINED_PLUS",
        "max_languages": "MAX_LANGUAGES",
        "max_links": "MAX_LINKS",
        "max_management_roles": "MAX_MANAGEMENT_ROLES",
        "max_owned": "MAX_OWNED",
        "max_roles": "MAX_ROLES"
    }
    __properties: ClassVar[List[str]] = ["CAPACITY", "GROUP_TRANSFER_REQUIREMENTS", "MAX_INVITES_REQUESTS", "MAX_JOINED", "MAX_JOINED_PLUS", "MAX_LANGUAGES", "MAX_LINKS", "MAX_MANAGEMENT_ROLES", "MAX_OWNED", "MAX_ROLES"]

    @classmethod
    def __preprocess_input_names(
        cls,
        obj: Any,
        remove_hidden_storage_names: bool = True,
    ) -> Any:
        if not isinstance(obj, _Mapping):
            return obj
        obj = dict(obj)
        if "CAPACITY" not in obj and "capacity" in obj:
            obj["CAPACITY"] = obj["capacity"]
        obj.pop("capacity", None)
        if "GROUP_TRANSFER_REQUIREMENTS" not in obj and "group_transfer_requirements" in obj:
            obj["GROUP_TRANSFER_REQUIREMENTS"] = obj["group_transfer_requirements"]
        obj.pop("group_transfer_requirements", None)
        if "MAX_INVITES_REQUESTS" not in obj and "max_invites_requests" in obj:
            obj["MAX_INVITES_REQUESTS"] = obj["max_invites_requests"]
        obj.pop("max_invites_requests", None)
        if "MAX_JOINED" not in obj and "max_joined" in obj:
            obj["MAX_JOINED"] = obj["max_joined"]
        obj.pop("max_joined", None)
        if "MAX_JOINED_PLUS" not in obj and "max_joined_plus" in obj:
            obj["MAX_JOINED_PLUS"] = obj["max_joined_plus"]
        obj.pop("max_joined_plus", None)
        if "MAX_LANGUAGES" not in obj and "max_languages" in obj:
            obj["MAX_LANGUAGES"] = obj["max_languages"]
        obj.pop("max_languages", None)
        if "MAX_LINKS" not in obj and "max_links" in obj:
            obj["MAX_LINKS"] = obj["max_links"]
        obj.pop("max_links", None)
        if "MAX_MANAGEMENT_ROLES" not in obj and "max_management_roles" in obj:
            obj["MAX_MANAGEMENT_ROLES"] = obj["max_management_roles"]
        obj.pop("max_management_roles", None)
        if "MAX_OWNED" not in obj and "max_owned" in obj:
            obj["MAX_OWNED"] = obj["max_owned"]
        obj.pop("max_owned", None)
        if "MAX_ROLES" not in obj and "max_roles" in obj:
            obj["MAX_ROLES"] = obj["max_roles"]
        obj.pop("max_roles", None)
        return obj

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        extra="forbid",
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model"""
        return pprint.pformat(self.to_dict())

    def __repr__(self) -> str:
        """For print and pprint"""
        return self.to_str()

    def __eq__(self, other: object) -> bool:
        """Returns true if both objects are equal"""
        if not isinstance(other, APIConfigConstantsGROUPS):
            return False

        return self.to_dict() == other.to_dict()

    def __ne__(self, other: object) -> bool:
        """Returns true if both objects are not equal"""
        if not isinstance(other, APIConfigConstantsGROUPS):
            return True

        return not (self == other)


    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        to_openapi_dict = _get_openapi_to_dict(self)
        if to_openapi_dict is not None:
            return json.dumps(to_jsonable_python(to_openapi_dict(self)))
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of APIConfigConstantsGROUPS from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, serialize: bool = False) -> Dict[str, Any]:
        """Return all declared model fields using public or wire names."""
        return {
            ("CAPACITY" if serialize else "capacity"): _to_legacy_value(getattr(self, "capacity", None), serialize),
            ("GROUP_TRANSFER_REQUIREMENTS" if serialize else "group_transfer_requirements"): _to_legacy_value(getattr(self, "group_transfer_requirements", None), serialize),
            ("MAX_INVITES_REQUESTS" if serialize else "max_invites_requests"): _to_legacy_value(getattr(self, "max_invites_requests", None), serialize),
            ("MAX_JOINED" if serialize else "max_joined"): _to_legacy_value(getattr(self, "max_joined", None), serialize),
            ("MAX_JOINED_PLUS" if serialize else "max_joined_plus"): _to_legacy_value(getattr(self, "max_joined_plus", None), serialize),
            ("MAX_LANGUAGES" if serialize else "max_languages"): _to_legacy_value(getattr(self, "max_languages", None), serialize),
            ("MAX_LINKS" if serialize else "max_links"): _to_legacy_value(getattr(self, "max_links", None), serialize),
            ("MAX_MANAGEMENT_ROLES" if serialize else "max_management_roles"): _to_legacy_value(getattr(self, "max_management_roles", None), serialize),
            ("MAX_OWNED" if serialize else "max_owned"): _to_legacy_value(getattr(self, "max_owned", None), serialize),
            ("MAX_ROLES" if serialize else "max_roles"): _to_legacy_value(getattr(self, "max_roles", None), serialize),
        }

    def __openapi_generator_modern_projection(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    setattr(
        to_dict,
        _OPENAPI_GENERATOR_TO_DICT,
        __openapi_generator_modern_projection,
    )
    setattr(
        __openapi_generator_modern_projection,
        _OPENAPI_GENERATOR_TO_DICT,
        to_dict,
    )
    del __openapi_generator_modern_projection

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of APIConfigConstantsGROUPS from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        obj = _cast(
            Dict[str, Any],
            cls.__preprocess_input_names(
                obj,
                remove_hidden_storage_names=True,
            ),
        )

        _obj = cls.model_validate({
            "CAPACITY": obj.get("CAPACITY") if obj.get("CAPACITY") is not None else 100000,
            "GROUP_TRANSFER_REQUIREMENTS": obj.get("GROUP_TRANSFER_REQUIREMENTS"),
            "MAX_INVITES_REQUESTS": obj.get("MAX_INVITES_REQUESTS") if obj.get("MAX_INVITES_REQUESTS") is not None else 50,
            "MAX_JOINED": obj.get("MAX_JOINED") if obj.get("MAX_JOINED") is not None else 100,
            "MAX_JOINED_PLUS": obj.get("MAX_JOINED_PLUS") if obj.get("MAX_JOINED_PLUS") is not None else 200,
            "MAX_LANGUAGES": obj.get("MAX_LANGUAGES") if obj.get("MAX_LANGUAGES") is not None else 10,
            "MAX_LINKS": obj.get("MAX_LINKS") if obj.get("MAX_LINKS") is not None else 3,
            "MAX_MANAGEMENT_ROLES": obj.get("MAX_MANAGEMENT_ROLES") if obj.get("MAX_MANAGEMENT_ROLES") is not None else 5,
            "MAX_OWNED": obj.get("MAX_OWNED") if obj.get("MAX_OWNED") is not None else 5,
            "MAX_ROLES": obj.get("MAX_ROLES") if obj.get("MAX_ROLES") is not None else 50
        })
        return _obj


