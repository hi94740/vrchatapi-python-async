# coding: utf-8

"""
    VRChat API Documentation


    The version of the OpenAPI document: 1.20.8
    Contact: vrchatapi.lpv0t@aries.fyi
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from collections.abc import Mapping as _Mapping
from pydantic import AliasChoices, BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Union, cast as _cast
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


_OPENAPI_GENERATOR_TO_DICT = "_openapi_generator_to_dict"


def _get_openapi_to_dict(value: Any) -> Any:
    # Reciprocal function references preserve inherited generated methods
    # without reserving model member names. Checking both directions also
    # rejects overrides whose decorators copy function attributes.
    to_dict = getattr(value, "to_dict", None)
    type_to_dict = getattr(type(value), "to_dict", None)
    if (
        not callable(to_dict)
        or getattr(to_dict, "__func__", None) is not type_to_dict
    ):
        return None

    openapi_to_dict = getattr(
        type_to_dict, _OPENAPI_GENERATOR_TO_DICT, None
    )
    if (
        not callable(openapi_to_dict)
        or getattr(
            openapi_to_dict,
            _OPENAPI_GENERATOR_TO_DICT,
            None,
        ) is not type_to_dict
    ):
        return None
    return openapi_to_dict


def _to_legacy_item(value: Any, serialize: bool) -> Any:
    to_dict = getattr(value, "to_dict", None)
    if _get_openapi_to_dict(value) is not None and callable(to_dict):
        return to_dict(serialize=serialize)
    if callable(to_dict):
        return to_dict()
    return value


def _to_legacy_value(value: Any, serialize: bool) -> Any:
    # python-legacy converted only immediate list elements or dictionary values:
    # https://github.com/OpenAPITools/openapi-generator/blob/c84b949df1a9ec04ba75989cb49b45c940c2f694/modules/openapi-generator/src/main/resources/python-legacy/model.mustache#L203-L231
    if isinstance(value, list):
        return [_to_legacy_item(item, serialize) for item in value]
    if isinstance(value, dict):
        return {
            key: _to_legacy_item(item, serialize)
            for key, item in value.items()
        }
    return _to_legacy_item(value, serialize)


def _to_openapi_value(value: Any) -> Any:
    if isinstance(value, list):
        return [_to_openapi_value(item) for item in value]
    if isinstance(value, dict):
        return {key: _to_openapi_value(item) for key, item in value.items()}

    to_openapi_dict = _get_openapi_to_dict(value)
    if to_openapi_dict is not None:
        return to_openapi_dict(value)

    to_dict = getattr(value, "to_dict", None)
    if callable(to_dict):
        return to_dict()
    return value


class TransactionAgreement(BaseModel):
    """
    Represents a single Transaction, which is likely between VRChat and Steam.

    Attributes:
      openapi_types (dict): The key is attribute name
                            and the value is attribute type.
      attribute_map (dict): The key is attribute name
                            and the value is json key in definition.
    """ # noqa: E501
    agreement: Optional[StrictStr] = None
    agreement_id: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, validation_alias=AliasChoices("agreementId", "agreement_id"), serialization_alias="agreementId")
    billing_type: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, validation_alias=AliasChoices("billingType", "billing_type"), serialization_alias="billingType")
    currency: Optional[Annotated[str, Field(min_length=1, strict=True)]] = None
    end_date: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, validation_alias=AliasChoices("endDate", "end_date"), serialization_alias="endDate")
    failed_attempts: Optional[StrictInt] = Field(default=None, validation_alias=AliasChoices("failedAttempts", "failed_attempts"), serialization_alias="failedAttempts")
    frequency: Optional[StrictInt] = None
    item_id: Optional[StrictInt] = Field(default=None, validation_alias=AliasChoices("itemId", "item_id"), serialization_alias="itemId")
    last_amount: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, validation_alias=AliasChoices("lastAmount", "last_amount"), serialization_alias="lastAmount")
    last_amount_vat: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, validation_alias=AliasChoices("lastAmountVat", "last_amount_vat"), serialization_alias="lastAmountVat")
    last_payment: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, validation_alias=AliasChoices("lastPayment", "last_payment"), serialization_alias="lastPayment")
    next_payment: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, validation_alias=AliasChoices("nextPayment", "next_payment"), serialization_alias="nextPayment")
    outstanding: Optional[StrictInt] = None
    period: Optional[Annotated[str, Field(min_length=1, strict=True)]] = None
    recurring_amt: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, validation_alias=AliasChoices("recurringAmt", "recurring_amt"), serialization_alias="recurringAmt")
    start_date: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, validation_alias=AliasChoices("startDate", "start_date"), serialization_alias="startDate")
    status: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, description="This is NOT TransactionStatus, but whatever Steam return.")
    time_created: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, validation_alias=AliasChoices("timeCreated", "time_created"), serialization_alias="timeCreated")
    openapi_types: ClassVar[Dict[str, str]] = {
        "agreement": "str",
        "agreement_id": "str",
        "billing_type": "str",
        "currency": "str",
        "end_date": "str",
        "failed_attempts": "int",
        "frequency": "int",
        "item_id": "int",
        "last_amount": "float",
        "last_amount_vat": "float",
        "last_payment": "str",
        "next_payment": "str",
        "outstanding": "int",
        "period": "str",
        "recurring_amt": "float",
        "start_date": "str",
        "status": "str",
        "time_created": "str"
    }

    attribute_map: ClassVar[Dict[str, str]] = {
        "agreement": "agreement",
        "agreement_id": "agreementId",
        "billing_type": "billingType",
        "currency": "currency",
        "end_date": "endDate",
        "failed_attempts": "failedAttempts",
        "frequency": "frequency",
        "item_id": "itemId",
        "last_amount": "lastAmount",
        "last_amount_vat": "lastAmountVat",
        "last_payment": "lastPayment",
        "next_payment": "nextPayment",
        "outstanding": "outstanding",
        "period": "period",
        "recurring_amt": "recurringAmt",
        "start_date": "startDate",
        "status": "status",
        "time_created": "timeCreated"
    }
    __properties: ClassVar[List[str]] = ["agreement", "agreementId", "billingType", "currency", "endDate", "failedAttempts", "frequency", "itemId", "lastAmount", "lastAmountVat", "lastPayment", "nextPayment", "outstanding", "period", "recurringAmt", "startDate", "status", "timeCreated"]

    @classmethod
    def __preprocess_input_names(
        cls,
        obj: Any,
        remove_hidden_storage_names: bool = True,
    ) -> Any:
        if not isinstance(obj, _Mapping):
            return obj
        obj = dict(obj)
        if "agreementId" not in obj and "agreement_id" in obj:
            obj["agreementId"] = obj["agreement_id"]
        obj.pop("agreement_id", None)
        if "billingType" not in obj and "billing_type" in obj:
            obj["billingType"] = obj["billing_type"]
        obj.pop("billing_type", None)
        if "endDate" not in obj and "end_date" in obj:
            obj["endDate"] = obj["end_date"]
        obj.pop("end_date", None)
        if "failedAttempts" not in obj and "failed_attempts" in obj:
            obj["failedAttempts"] = obj["failed_attempts"]
        obj.pop("failed_attempts", None)
        if "itemId" not in obj and "item_id" in obj:
            obj["itemId"] = obj["item_id"]
        obj.pop("item_id", None)
        if "lastAmount" not in obj and "last_amount" in obj:
            obj["lastAmount"] = obj["last_amount"]
        obj.pop("last_amount", None)
        if "lastAmountVat" not in obj and "last_amount_vat" in obj:
            obj["lastAmountVat"] = obj["last_amount_vat"]
        obj.pop("last_amount_vat", None)
        if "lastPayment" not in obj and "last_payment" in obj:
            obj["lastPayment"] = obj["last_payment"]
        obj.pop("last_payment", None)
        if "nextPayment" not in obj and "next_payment" in obj:
            obj["nextPayment"] = obj["next_payment"]
        obj.pop("next_payment", None)
        if "recurringAmt" not in obj and "recurring_amt" in obj:
            obj["recurringAmt"] = obj["recurring_amt"]
        obj.pop("recurring_amt", None)
        if "startDate" not in obj and "start_date" in obj:
            obj["startDate"] = obj["start_date"]
        obj.pop("start_date", None)
        if "timeCreated" not in obj and "time_created" in obj:
            obj["timeCreated"] = obj["time_created"]
        obj.pop("time_created", None)
        return obj

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        extra="forbid",
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model"""
        return pprint.pformat(self.to_dict())

    def __repr__(self) -> str:
        """For print and pprint"""
        return self.to_str()

    def __eq__(self, other: object) -> bool:
        """Returns true if both objects are equal"""
        if not isinstance(other, TransactionAgreement):
            return False

        return self.to_dict() == other.to_dict()

    def __ne__(self, other: object) -> bool:
        """Returns true if both objects are not equal"""
        if not isinstance(other, TransactionAgreement):
            return True

        return not (self == other)


    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        to_openapi_dict = _get_openapi_to_dict(self)
        if to_openapi_dict is not None:
            return json.dumps(to_jsonable_python(to_openapi_dict(self)))
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TransactionAgreement from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, serialize: bool = False) -> Dict[str, Any]:
        """Return all declared model fields using public or wire names."""
        return {
            ("agreement" if serialize else "agreement"): _to_legacy_value(getattr(self, "agreement", None), serialize),
            ("agreementId" if serialize else "agreement_id"): _to_legacy_value(getattr(self, "agreement_id", None), serialize),
            ("billingType" if serialize else "billing_type"): _to_legacy_value(getattr(self, "billing_type", None), serialize),
            ("currency" if serialize else "currency"): _to_legacy_value(getattr(self, "currency", None), serialize),
            ("endDate" if serialize else "end_date"): _to_legacy_value(getattr(self, "end_date", None), serialize),
            ("failedAttempts" if serialize else "failed_attempts"): _to_legacy_value(getattr(self, "failed_attempts", None), serialize),
            ("frequency" if serialize else "frequency"): _to_legacy_value(getattr(self, "frequency", None), serialize),
            ("itemId" if serialize else "item_id"): _to_legacy_value(getattr(self, "item_id", None), serialize),
            ("lastAmount" if serialize else "last_amount"): _to_legacy_value(getattr(self, "last_amount", None), serialize),
            ("lastAmountVat" if serialize else "last_amount_vat"): _to_legacy_value(getattr(self, "last_amount_vat", None), serialize),
            ("lastPayment" if serialize else "last_payment"): _to_legacy_value(getattr(self, "last_payment", None), serialize),
            ("nextPayment" if serialize else "next_payment"): _to_legacy_value(getattr(self, "next_payment", None), serialize),
            ("outstanding" if serialize else "outstanding"): _to_legacy_value(getattr(self, "outstanding", None), serialize),
            ("period" if serialize else "period"): _to_legacy_value(getattr(self, "period", None), serialize),
            ("recurringAmt" if serialize else "recurring_amt"): _to_legacy_value(getattr(self, "recurring_amt", None), serialize),
            ("startDate" if serialize else "start_date"): _to_legacy_value(getattr(self, "start_date", None), serialize),
            ("status" if serialize else "status"): _to_legacy_value(getattr(self, "status", None), serialize),
            ("timeCreated" if serialize else "time_created"): _to_legacy_value(getattr(self, "time_created", None), serialize),
        }

    def __openapi_generator_modern_projection(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    setattr(
        to_dict,
        _OPENAPI_GENERATOR_TO_DICT,
        __openapi_generator_modern_projection,
    )
    setattr(
        __openapi_generator_modern_projection,
        _OPENAPI_GENERATOR_TO_DICT,
        to_dict,
    )
    del __openapi_generator_modern_projection

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TransactionAgreement from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        obj = _cast(
            Dict[str, Any],
            cls.__preprocess_input_names(
                obj,
                remove_hidden_storage_names=True,
            ),
        )

        _obj = cls.model_validate({
            "agreement": obj.get("agreement"),
            "agreementId": obj.get("agreementId"),
            "billingType": obj.get("billingType"),
            "currency": obj.get("currency"),
            "endDate": obj.get("endDate"),
            "failedAttempts": obj.get("failedAttempts"),
            "frequency": obj.get("frequency"),
            "itemId": obj.get("itemId"),
            "lastAmount": obj.get("lastAmount"),
            "lastAmountVat": obj.get("lastAmountVat"),
            "lastPayment": obj.get("lastPayment"),
            "nextPayment": obj.get("nextPayment"),
            "outstanding": obj.get("outstanding"),
            "period": obj.get("period"),
            "recurringAmt": obj.get("recurringAmt"),
            "startDate": obj.get("startDate"),
            "status": obj.get("status"),
            "timeCreated": obj.get("timeCreated")
        })
        return _obj


