# coding: utf-8

"""
    VRChat API Documentation


    The version of the OpenAPI document: 1.20.8
    Contact: vrchatapi.lpv0t@aries.fyi
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from collections.abc import Mapping as _Mapping
from datetime import datetime
from pydantic import AliasChoices, BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional, cast as _cast
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python


_OPENAPI_GENERATOR_TO_DICT = "_openapi_generator_to_dict"


def _get_openapi_to_dict(value: Any) -> Any:
    # Reciprocal function references preserve inherited generated methods
    # without reserving model member names. Checking both directions also
    # rejects overrides whose decorators copy function attributes.
    to_dict = getattr(value, "to_dict", None)
    type_to_dict = getattr(type(value), "to_dict", None)
    if (
        not callable(to_dict)
        or getattr(to_dict, "__func__", None) is not type_to_dict
    ):
        return None

    openapi_to_dict = getattr(
        type_to_dict, _OPENAPI_GENERATOR_TO_DICT, None
    )
    if (
        not callable(openapi_to_dict)
        or getattr(
            openapi_to_dict,
            _OPENAPI_GENERATOR_TO_DICT,
            None,
        ) is not type_to_dict
    ):
        return None
    return openapi_to_dict


def _to_legacy_item(value: Any, serialize: bool) -> Any:
    to_dict = getattr(value, "to_dict", None)
    if _get_openapi_to_dict(value) is not None and callable(to_dict):
        return to_dict(serialize=serialize)
    if callable(to_dict):
        return to_dict()
    return value


def _to_legacy_value(value: Any, serialize: bool) -> Any:
    # python-legacy converted only immediate list elements or dictionary values:
    # https://github.com/OpenAPITools/openapi-generator/blob/c84b949df1a9ec04ba75989cb49b45c940c2f694/modules/openapi-generator/src/main/resources/python-legacy/model.mustache#L203-L231
    if isinstance(value, list):
        return [_to_legacy_item(item, serialize) for item in value]
    if isinstance(value, dict):
        return {
            key: _to_legacy_item(item, serialize)
            for key, item in value.items()
        }
    return _to_legacy_item(value, serialize)


def _to_openapi_value(value: Any) -> Any:
    if isinstance(value, list):
        return [_to_openapi_value(item) for item in value]
    if isinstance(value, dict):
        return {key: _to_openapi_value(item) for key, item in value.items()}

    to_openapi_dict = _get_openapi_to_dict(value)
    if to_openapi_dict is not None:
        return to_openapi_dict(value)

    to_dict = getattr(value, "to_dict", None)
    if callable(to_dict):
        return to_dict()
    return value


class JamStateChangeDates(BaseModel):
    """
    JamStateChangeDates

    Attributes:
      openapi_types (dict): The key is attribute name
                            and the value is attribute type.
      attribute_map (dict): The key is attribute name
                            and the value is json key in definition.
    """ # noqa: E501
    closed: Optional[datetime] = None
    submissions_closed: Optional[datetime] = Field(default=None, validation_alias=AliasChoices("submissionsClosed", "submissions_closed"), serialization_alias="submissionsClosed")
    submissions_opened: Optional[datetime] = Field(default=None, validation_alias=AliasChoices("submissionsOpened", "submissions_opened"), serialization_alias="submissionsOpened")
    winners_selected: Optional[datetime] = Field(default=None, validation_alias=AliasChoices("winnersSelected", "winners_selected"), serialization_alias="winnersSelected")
    openapi_types: ClassVar[Dict[str, str]] = {
        "closed": "datetime",
        "submissions_closed": "datetime",
        "submissions_opened": "datetime",
        "winners_selected": "datetime"
    }

    attribute_map: ClassVar[Dict[str, str]] = {
        "closed": "closed",
        "submissions_closed": "submissionsClosed",
        "submissions_opened": "submissionsOpened",
        "winners_selected": "winnersSelected"
    }
    __properties: ClassVar[List[str]] = ["closed", "submissionsClosed", "submissionsOpened", "winnersSelected"]

    @classmethod
    def __preprocess_input_names(
        cls,
        obj: Any,
        remove_hidden_storage_names: bool = True,
    ) -> Any:
        if not isinstance(obj, _Mapping):
            return obj
        obj = dict(obj)
        if "submissionsClosed" not in obj and "submissions_closed" in obj:
            obj["submissionsClosed"] = obj["submissions_closed"]
        obj.pop("submissions_closed", None)
        if "submissionsOpened" not in obj and "submissions_opened" in obj:
            obj["submissionsOpened"] = obj["submissions_opened"]
        obj.pop("submissions_opened", None)
        if "winnersSelected" not in obj and "winners_selected" in obj:
            obj["winnersSelected"] = obj["winners_selected"]
        obj.pop("winners_selected", None)
        return obj

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        extra="forbid",
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model"""
        return pprint.pformat(self.to_dict())

    def __repr__(self) -> str:
        """For print and pprint"""
        return self.to_str()

    def __eq__(self, other: object) -> bool:
        """Returns true if both objects are equal"""
        if not isinstance(other, JamStateChangeDates):
            return False

        return self.to_dict() == other.to_dict()

    def __ne__(self, other: object) -> bool:
        """Returns true if both objects are not equal"""
        if not isinstance(other, JamStateChangeDates):
            return True

        return not (self == other)


    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        to_openapi_dict = _get_openapi_to_dict(self)
        if to_openapi_dict is not None:
            return json.dumps(to_jsonable_python(to_openapi_dict(self)))
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of JamStateChangeDates from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, serialize: bool = False) -> Dict[str, Any]:
        """Return all declared model fields using public or wire names."""
        return {
            ("closed" if serialize else "closed"): _to_legacy_value(getattr(self, "closed", None), serialize),
            ("submissionsClosed" if serialize else "submissions_closed"): _to_legacy_value(getattr(self, "submissions_closed", None), serialize),
            ("submissionsOpened" if serialize else "submissions_opened"): _to_legacy_value(getattr(self, "submissions_opened", None), serialize),
            ("winnersSelected" if serialize else "winners_selected"): _to_legacy_value(getattr(self, "winners_selected", None), serialize),
        }

    def __openapi_generator_modern_projection(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if closed (nullable) is None
        # and model_fields_set contains the field
        if self.closed is None and "closed" in self.model_fields_set:
            _dict['closed'] = None

        # set to None if submissions_closed (nullable) is None
        # and model_fields_set contains the field
        if self.submissions_closed is None and "submissions_closed" in self.model_fields_set:
            _dict['submissionsClosed'] = None

        # set to None if submissions_opened (nullable) is None
        # and model_fields_set contains the field
        if self.submissions_opened is None and "submissions_opened" in self.model_fields_set:
            _dict['submissionsOpened'] = None

        # set to None if winners_selected (nullable) is None
        # and model_fields_set contains the field
        if self.winners_selected is None and "winners_selected" in self.model_fields_set:
            _dict['winnersSelected'] = None

        return _dict

    setattr(
        to_dict,
        _OPENAPI_GENERATOR_TO_DICT,
        __openapi_generator_modern_projection,
    )
    setattr(
        __openapi_generator_modern_projection,
        _OPENAPI_GENERATOR_TO_DICT,
        to_dict,
    )
    del __openapi_generator_modern_projection

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of JamStateChangeDates from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        obj = _cast(
            Dict[str, Any],
            cls.__preprocess_input_names(
                obj,
                remove_hidden_storage_names=True,
            ),
        )

        _obj = cls.model_validate({
            "closed": obj.get("closed"),
            "submissionsClosed": obj.get("submissionsClosed"),
            "submissionsOpened": obj.get("submissionsOpened"),
            "winnersSelected": obj.get("winnersSelected")
        })
        return _obj


